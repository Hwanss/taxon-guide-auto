name: TaxonGuru Bilingual Story Auto-Publish (CONTROLLED)

# 직접 신규 발행을 실행하지 않습니다.
# 기존 정리 완료 여부를 판단한 뒤 자동으로 신규 발행하는 작업은
# 'TaxonGuru Master Cleanup & Publish'가 담당합니다.
on:
  workflow_dispatch:

jobs:
  controlled-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Controlled by master pipeline
        run: |
          echo "이 Action은 마스터 파이프라인에 통합되었습니다."
          echo "TaxonGuru Master Cleanup & Publish만 사용하세요."
