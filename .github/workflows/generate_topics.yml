name: TaxonGuru Topic Planner (CONTROLLED)

# 대기 주제가 10건 미만일 때 마스터 파이프라인이 자동 실행합니다.
# 평상시 사용자가 직접 실행할 필요가 없습니다.
on:
  workflow_dispatch:

jobs:
  controlled-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Controlled by master pipeline
        run: |
          echo "주제 생성은 마스터 파이프라인이 대기 목록 수를 확인해 자동 실행합니다."
          echo "TaxonGuru Master Cleanup & Publish만 사용하세요."
